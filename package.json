{
  "name": "umipig-dental-clinic",
  "version": "1.0.0",
  "description": "Umipig Dental Clinic - Modern Dental Care Management System",
  "main": "index.php",
  "scripts": {
    "start": "node -e \"const { exec } = require('child_process'); exec('php -v', (error, stdout, stderr) => { if (error) { console.error('❌ PHP is not installed or not in your PATH. Please install PHP from https://windows.php.net/download/ and add it to your system PATH.'); process.exit(1); } else { console.log('✅ PHP is installed:', stdout.split('\n')[0]); console.log('🚀 Starting PHP development server on http://localhost:8000'); require('child_process').exec('php -S localhost:8000'); } });\"",
    "dev": "concurrently \"npm run start\" \"npm run watch-css\"",
    "watch-css": "npx tailwindcss -i ./assets/css/input.css -o ./assets/css/output.css --watch",
    "build-css": "npx tailwindcss -i ./assets/css/input.css -o ./assets/css/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/krissttian1/Umipig-Dental-Clinic.git"
  },
  "keywords": [
    "dental",
    "clinic",
    "healthcare",
    "php",
    "tailwindcss"
  ],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/krissttian1/Umipig-Dental-Clinic/issues"
  },
  "homepage": "https://github.com/krissttian1/Umipig-Dental-Clinic#readme",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18"
  }
}
